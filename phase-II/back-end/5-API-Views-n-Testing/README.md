# API View

- Create DRF APIView endpoints that return all records or one specific record.
- Connect URL paths to class-based views using .as_view().
- Use path parameters to retrieve a Pokémon or move by ID or name.
- Serialize Django model instances into JSON responses.
- Explain how HTTP methods relate to CRUD operations.
- Test API endpoints using Django’s Client, reverse(), fixtures, and expected response data.
- Identify common endpoint errors, including invalid IDs, database lookup failures, and Method Not Allowed responses.