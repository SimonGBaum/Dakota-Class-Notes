# When to use REM or other units in CSS

- Use rem for font sizes, margins, padding, gaps, and many component dimensions.
- Use %, fr, vw, or max-width for layouts that should adapt to the available space.
- Use px for very small fixed details such as 1px borders, shadows, or precise icon strokes.
- Use vh/dvh for viewport-height sections when appropriate.